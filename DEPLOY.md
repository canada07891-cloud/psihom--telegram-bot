# Деплой ПСИХОМИР бота на Render.com

## Шаг 1: Загрузка на GitHub

Если у вас еще нет GitHub аккаунта, создайте его на [github.com](https://github.com)

### 1.1. Создайте новый репозиторий на GitHub

1. Откройте [github.com/new](https://github.com/new)
2. Назовите репозиторий: `psihomир-telegram-bot`
3. Оставьте его **Public** (публичным)
4. **НЕ** добавляйте README, .gitignore или лицензию (у нас уже есть)
5. Нажмите **Create repository**

### 1.2. Подключите локальный репозиторий к GitHub

Скопируйте URL вашего нового репозитория (будет выглядеть как `https://github.com/ВАШ_USERNAME/psihomір-telegram-bot.git`)

Выполните команды:

```bash
git remote add origin https://github.com/ВАШ_USERNAME/psihomір-telegram-bot.git
git push -u origin main
```

## Шаг 2: Деплой на Render.com

### 2.1. Создайте аккаунт на Render.com

1. Перейдите на [render.com](https://render.com)
2. Нажмите **Get Started** или **Sign Up**
3. Войдите через GitHub (это упростит подключение репозиториев)

### 2.2. Создайте новый Web Service

1. На главной странице Render нажмите **New +** → **Web Service**
2. Найдите ваш репозиторий `psihomір-telegram-bot` и нажмите **Connect**
3. Заполните настройки:

**Основные настройки:**
- **Name**: `psihomир-bot` (или любое имя)
- **Region**: выберите ближайший регион (Europe)
- **Branch**: `main`
- **Root Directory**: оставьте пустым
- **Runtime**: `Node`
- **Build Command**: `npm install`
- **Start Command**: `npm start`

**План:**
- Выберите **Free** (бесплатный план)

### 2.3. Добавьте переменные окружения

Прокрутите вниз до раздела **Environment Variables** и добавьте:

1. Нажмите **Add Environment Variable**
2. Добавьте:
   - **Key**: `BOT_TOKEN`
   - **Value**: `ваш_токен_бота` (из .env файла)

3. Снова нажмите **Add Environment Variable**
4. Добавьте:
   - **Key**: `ADMIN_CHAT_ID`
   - **Value**: `ваш_chat_id` (из .env файла)

### 2.4. Деплой

1. Нажмите **Create Web Service**
2. Render начнёт деплой (займет 2-5 минут)
3. После завершения статус изменится на **Live** 🟢

## Готово!

Ваш бот теперь работает 24/7 на Render.com!

### Важные заметки:

- **Бесплатный план Render** усыпляет сервис после 15 минут неактивности
- При первом сообщении боту после сна может быть задержка 30-60 секунд
- Для постоянной работы без засыпания нужен платный план ($7/месяц)

### Просмотр логов

Чтобы увидеть что происходит с ботом:
1. Зайдите в ваш Web Service на Render
2. Перейдите на вкладку **Logs**
3. Вы увидите консольный вывод бота

### Обновление бота

Когда вы меняете код:

1. Сделайте коммит и push:
```bash
git add .
git commit -m "Описание изменений"
git push
```

2. Render автоматически задеплоит новую версию!

## Альтернатива: Запуск на VPS (DigitalOcean, AWS и т.д.)

Если у вас есть свой сервер, просто:

```bash
# Клонируйте репозиторий
git clone https://github.com/ВАШ_USERNAME/psihomір-telegram-bot.git
cd psihomір-telegram-bot

# Установите зависимости
npm install

# Создайте .env файл
nano .env
# Вставьте ваши BOT_TOKEN и ADMIN_CHAT_ID

# Запустите с PM2 для постоянной работы
npm install -g pm2
pm2 start bot.js --name psihomір-bot
pm2 save
pm2 startup
```
